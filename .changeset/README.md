# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

When you make a user-facing change, run `npm run changeset` and follow the
prompts to describe it. The release workflow consumes these files to bump the
version, update `CHANGELOG.md`, and publish to npm.
