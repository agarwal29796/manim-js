## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] `npm run build && npm run check:exports` passes
- [ ] Added a changeset (`npm run changeset`) if this is user-facing
- [ ] `src/core/` stays dependency-free and DOM-free
